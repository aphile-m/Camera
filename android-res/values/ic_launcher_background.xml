<?xml version="1.0" encoding="utf-8"?>
<!-- Overwrites Capacitor's default (#FFFFFF). Defined in its own file, as the
     template does, so it replaces rather than duplicates the resource. -->
<resources>
    <color name="ic_launcher_background">#100F0D</color>
</resources>
